容器的做法：把代码、运行环境、系统库、配置全部打包成一个“镜像”。

# docker pull下载镜像软件包
docker pull --platform=xxxxxx nginx 
（填linux/amd64 linux/arm64 linux/arm/v7 linux/arm/v6）

-re删除
# docker re删除所有下载过的容器
# docker re -f强制删除下载过的容器

# docker images显示所有下载过的镜像
# docker rei删除所有下载过的镜像
# docker rei image id删除指定的下载过的镜像
# docker run nginx使用镜像创建并运行容器（可换成镜像id)即基于该镜像创建的容器
# docker ps表示显示正在运行的容器进程状况
container id是每创建一个容器就自动分配一个id，names是容器的名字（不指定容器名字⑩自动分配一个名字）

run命令
-d
# docker run -d nginx(-d加了d表示让容器在后台执行，不会占用终端界面）
# docker run -d --name 容器名字my_nginx nginx基于哪个镜像模板 （自定义容器的名字）命令规则一级命令前面一个-，到二级命令时两个--
# docker run -it --rm alpine       (-it作用是可以让我的控制台进入容器进行交互，--rm当容器停止的时候删掉容器)alpine是一个轻量级linux系统
exit退出，可以用来临时调试一个容器
# docker run -d --restart always nginx          (表示只要容器停止了就会立即重启)宿主机断电等情况被动停止
# docker run -d --restart unless-stopped nginx   (表示手动停止的容器就不会尝试重启了)主动停止，docker使用这条命令会自动重启因意外停止的容器

容器调试
docker run是从镜像创建并运行容器，每次执行run就会创建一个容器，所以不想创建新容器
只想对原有容器进行启停，使用docker start 容器id/名字，，docker stop 容器id/名字 停止容器恶的运行


-p端口映射
# docker run -p 80:80端口映射（冒号前的是宿主机的端口，冒号后是容器内的端口）把容器和宿主机端口绑定
每个doker容器都运行在一个独立的虚拟环境中，容器的网路与宿主机是隔离的，并不能直接从宿主机访问到docker的内部网络
从宿主机web访问容器时,web里输入的是linux下的ip加端口护送


区分盘，卷，文件夹,文件，盘（Disk）、卷（Volume）和文件夹（Directory/Folder）是三个不同层级的概念。它们就像是“大楼”、“房间”和“收纳盒”
盘（Disk）：物理硬件本质：一块具体的物理芯片或磁盘片。
卷（Volume）：逻辑分区，本质：操作系统“眼里”的一个独立存储空间。每个卷都有自己独立的文件系统（如 NTFS, exFAT）。如果一个卷损坏或格式化，通常不会影响同一个盘上的其他卷。
文件夹（Folder / Directory）本质：卷里面的“容器”。特点：它不能独立存在，必须依附于某个特定的“卷”内。
挂载卷
目录挂载-v     目录挂载就是 把容器和宿主机的文件目录绑定，这种目录也叫挂载卷，操作（容器内对这个文件夹的修改会导致宿主机对应的文件夹修改，宿主机对这个文件夹的修改会导致容器内的对应文件夹修改），
挂载卷的作用是数据的持久化保存，因为当我们删除了容器，容器内的所有数据会删除，
当使用了挂载卷，容器内对应目录的数据就会保存在宿主机对应目录内，当我们删除了容器，数据不会被删除。所以是对数据做了持久化保存
# docker run -v 宿主机目录：容器内目录

-v绑定挂载
让docker自动创建一个存储空间，即一个文件夹，命名卷volume
# docker run -v 卷的名字：容器内目录，，，，这种挂载方式叫命名卷挂载（这个卷是在宿主机里）
# docker volume inspect 挂载卷名字       显示某个挂载卷是在宿主机的的真实目录哪个位置

进入目录前要切换成root用户，使用sudo -i
然后使用cd 要进去的目录
root: cd /var/lib/docker/volumes/nginx_html/_data
root: /var/lib/docker/volumes/nginx_html/_data# vi index(输入vi index打开index文件)vi是Linux下的命令行界面的文件编辑器
命名卷的功能，命名卷第一次使用的时候，docker会把容器的文件夹同步到宿主机命名卷里面初始化，命名卷里边就已经有了一些数据了
wq!
# docker volume list      列出所有创建过的卷
# docker volume rm 卷名称 删除一个卷
# docker volume prune -a 删除所有没有任何容器在使用的卷  （注意卷一定是宿主机里的存储空间的概念）

实例的概念

# docker run -e（用来往容器里传递环境变量）
启动一个mongodb的容器，mongodb是一个数据库，使用时需要对它设置账号名密码，
使用两个-e 把数据库的账号密码作为环境变量传递进去，启动时候数据库就会预设上账号密码（这些是在服务器的容器里操作的）
-e mongo_INITD_root_username=tech \
-e mongo_INITD_root_passwerd=shrimp \
mongo
启动以后，在windows宿主机终端运行mongosh "mongodb://tech:shrimp@111.231.100.92:27017"连接数据库
                                                              服务器的IP1地址（这一步是在主机访问服务器的数据库）


容器


